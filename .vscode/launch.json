{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "API Debug",
      "skipFiles": [
        "<node_internals>/**",
        "${workspaceFolder}/node_modules/**",
      ],
      "request": "launch",
      "command": "npm run start:debug",
      "cwd": "${workspaceFolder}"
    }
  ]
}